{
  "name": "my-slidev-presentations",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "npm run build:sre-next-2025 && npm run build:slidev-system && npm run build:index",
    "build:sre-next-2025": "cd sre-next-2025/src && npm install && npm run build",
    "build:slidev-system": "cd slidev-system/src && npm install && npm run build",
    "build:index": "node scripts/build-index.js",
    "dev:sre-next-2025": "cd sre-next-2025/src && npm run dev",
    "dev:slidev-system": "cd slidev-system/src && npm run dev"
  },
  "devDependencies": {
    "playwright-chromium": "^1.45.1"
  }
}
